# Diwali_Sales_Analysis_2k25
I completed an end-to-end analysis using Python (Pandas/Seaborn) for cleaning and EDA. I replicated the analysis in Excel (Pivot Tables) and queried the data in MySQL (GROUP BY, SUM). Finally, I built an interactive dashboard in Tableau to present the insights.
